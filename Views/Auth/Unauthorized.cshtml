@{
    ViewData["Title"] = "Không có quyền truy cập";
    Layout = "_Layout_authen";
}



<div class="error-container">
    <div class="error-code">403</div>
    <div class="error-message">
        <h2>🚫 Bạn không có quyền truy cập trang này.</h2>
        <p>Vui lòng đăng nhập bằng tài khoản <strong>Admin</strong> để tiếp tục.</p>
    </div>
    <a asp-controller="Home" asp-action="Index" class="btn btn-secondary">Quay lại trang chủ</a>
    <a asp-controller="Auth" asp-action="Login" class="btn btn-primary">Đăng nhập</a>
</div>


<style>
    body {
        background: linear-gradient(135deg, #ff6e7f, #bfe9ff);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0;
    }

    .error-container {
        text-align: center;
        color: #fff;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
    }

    .error-code {
        font-size: 12rem;
        font-weight: bold;
        margin-bottom: 20px;
        animation: pulse 2s infinite ease-in-out;
    }

    .error-message {
        font-size: 2rem;
        margin-bottom: 30px;
    }

    .btn-home {
        background-color: #fff;
        color: #ff6e7f;
        padding: 15px 30px;
        border-radius: 30px;
        font-size: 1.2rem;
        text-decoration: none;
        transition: background-color 0.3s ease;
    }

    .btn-home:hover {
        background-color: #f0f0f0;
    }

    @* @keyframes pulse {
        0% {
            transform: scale(1);
        }

        50% {
            transform: scale(1.1);
        }

        100% {
            transform: scale(1);
        }
    } *@
</style>